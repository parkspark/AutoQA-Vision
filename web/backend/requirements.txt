# DNF Vision 풀스택 web/ 백엔드(FastAPI) 전용 의존성.
# 루트 requirements.txt(streamlit·PyQt6·jupyter·GPU onnx 포함)와 달리 추론 서버에 필요한 것만 담는다.
# torch는 CPU 빌드(pip 기본 인덱스)로 설치된다 — yolo11s 추론은 CPU로도 데모에 충분하고,
# CUDA 버전 불일치 없이 어떤 머신에서도 재현된다. GPU 가속이 필요하면 본인 CUDA에 맞는
# torch를 https://pytorch.org/get-started/locally/ 에서 따로 설치할 것.

fastapi==0.137.0
uvicorn==0.49.0
python-multipart==0.0.32
pillow==12.2.0
opencv-python==4.13.0.92
ultralytics==8.4.67
